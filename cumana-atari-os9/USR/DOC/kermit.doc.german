-------------------------------------------------------------------------Wolfgang Ocker                                                im Mai 1987-------------------------------------------------------------------------                             KERMITKERMIT  ist  ein Datenuebertragungsprogramm, das auf nahezu allenComputern  zur  Verfuegung  steht.  Eine  kurze  Beschreibung derOptionen erhalten Sie mit   kermit -? Ein paar Beispiele:-------------------(1) kermit -l=/t5 -s brief.doc CPROGS/test.c *.h     sendet  die  Dateien  "brief.doc",  "CPROGS/test.c"  und alle    ".h"-Files  ueber  die  serielle Schnittstelle /t5. Es werden    nur ASCII-Zeichen uebertragen.  (2) kermit -l=/t4 -r     empfaengt  eine/mehrere  Datei(en)  ueber  die  Schnittstelle    /t4, eben- falls im ASCII-Modus.  (3) kermit -l=/t5 arc hack -s -i bzw. kermit -ri -l=/t4     wie (1) bzw. (2), nur mit binaerer Datenuebertragung.  Sie   koennen   aber   auch  auf  dem  Remote-System  einen  sog."Remote-KERMIT"    starten.    Sie    loggen   sich   mit   einemKommunikationsprogramm  (oder  auch  mit  "kermit -cl/t5") in demanderen System ein, und rufen dort den KERMIT wie folgt auf: (4) kermit -h bzw. kermit -h -i     startet    einen    Remote-Kermit    (ASCII    bzw.   binaere    Uebertragung).  Anschliessend  verlassen  Sie  das  Terminalprogramm. Sie koennennun den eben gestarteten KERMIT "fernsteuern": (5) kermit -s -l=/t5 pss.doc pss.help bzw. kermit -sil=/t5 *.arc     uebetraegt  Dateien  an  den  Remote-Computer.  Diesen werden    dort im aktuellen Daten-Directory abgelegt.  (6)  kermit  -l=/t5  -g  record  "*wolfi"  bzw. kermit -l=/t5 -ig"save/*"     holt  sich  Dateien  vom  Remote-System.  Wenn Sie Wildchards    verwenden    wollen,   muessen   Sie   diese   Parameter   in    Gaensefuesschen (") ein- schliessen.  (weo) 